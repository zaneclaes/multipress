server {
    listen {{SERVER_PORT}};

    access_log {{NGINX_ACCESS_LOG}};
    error_log {{NGINX_ERROR_LOG}};

    # Everything is a 404
    location / {
        return 404;
    }

    # Prevent return 404 recursion.
    location = /404.html {
        internal;
    }
}
